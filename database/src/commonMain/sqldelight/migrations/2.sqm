CREATE TABLE track_play_record (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    track_id TEXT NOT NULL,
    track_number INTEGER NOT NULL,
    track_title TEXT NOT NULL,
    album_id TEXT NOT NULL,
    timestamp TEXT NOT NULL
);
